import SectionContainer from "@/components/helpers/section-container";
import SectionWrapper from "@/components/helpers/section-wrapper";

export default function Page() {
  return (
    <div className="bg-[url('/line-01.svg')] bg-repeat h-screen min-h-screen w-full">
      <SectionContainer>
        <SectionWrapper>asdsa</SectionWrapper>
      </SectionContainer>
    </div>
  );
}
